class Solution {
public:
    int largestAltitude(vector<int>& gain) {
        int sum = 0;
        int res = sum;
        
        for(int i : gain) {
              sum += i;
              res = max(sum, res);
        }
        return res;
    }
};