class Solution {
public:
    int sumOfGoodIntegers(int n, int k) {
        int sum = 0;
        int x;
        int maxLim = n+k;
        for(int x = 1; x <= maxLim; x++) {
        if(abs(n-x) <= k && (n & x) == 0) {
            sum += x;
        } 
        }
        return sum;
    }
};