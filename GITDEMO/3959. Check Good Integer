class Solution {
public:
    bool checkGoodInteger(int n) {
        int digitSum = 0;
        int sqSum = 0;
        while(n > 0) {
        int dig = n%10;
           digitSum += dig;
            sqSum += dig*dig;
            n/=10;
        }
        return (sqSum - digitSum) >= 50;
    }
};